/*
        primitive data types -> are call by value

        7 types : string,Number,Boolean,null = empty,undefined = variable is declared but what value it comes it not defined,symbol,bigInt

java script is dynamically typed language
        non primitive or reference type

        Array,Objects,Functions
         to declare symbols const id = Symbol('123')

        
*/

// const id = Symbol('123') != const id1 = Symbol('123')


const heros = ["a","b","c"] //-> Array 

let myObj = {
        name: "prajwal",    // ->objects
        age:19,
}

const myFunction = function(){
        console.log("hai")
}